########################################################################################
AUTOGENERATED HYPR CONFIG.
PLEASE USE THE CONFIG PROVIDED IN THE GIT REPO /examples/hypr.conf AND EDIT IT,
OR EDIT THIS ONE ACCORDING TO THE WIKI INSTRUCTIONS.
########################################################################################

#
# Please note not all available settings / options are set here.
# For a full list, see the wiki
#


# See https://wiki.hyprland.org/Configuring/Monitors/
monitor=DP-2, 1920x1080@59.94000, 0x1081, 1
monitor=DP-1, 5120x1440@143.97900, 1921x1081, 1
monitor=HDMI-A-1, 2560x1080@59.97800, 1000x0, 1

windowrulev2 = opacity 0.0 override 0.0 override,class:^(xwaylandvideobridge)$
windowrulev2 = noanim,class:^(xwaylandvideobridge)$
windowrulev2 = nofocus,class:^(xwaylandvideobridge)$
windowrulev2 = noinitialfocus,class:^(xwaylandvideobridge)$


# See https://wiki.hyprland.org/Configuring/Keywords/ for more

# Execute your favorite apps at launch
exec-once = hyprpaper & dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP 

# Source a file (multi-file configs)
# source = ~/.config/hypr/myColors.conf

# Some default env vars.
env = XCURSOR_SIZE,20

# For all categories, see https://wiki.hyprland.org/Configuring/Variables/
input {
    kb_layout = us,de
    kb_variant =
    kb_model =
    kb_options = 
    kb_rules =
    
    follow_mouse = 1

    touchpad {
        natural_scroll = no
    }

    sensitivity = 0 # -1.0 - 1.0, 0 means no modification.
}

device:glove80-left-keyboard {
    kb_layout = us,de
    kb_variant =
    kb_model =
    kb_options = grp:ralt_shift_toggle
    kb_rules =

}

general {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more

    gaps_in = 10
    gaps_out = 15
    border_size = 2
    col.active_border = rgb(155178) rgb(10834B) rgb(155178) 60deg
    col.inactive_border = rgba(595959aa)
    resize_on_border = false 

    layout = dwindle
}

decoration {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more

    rounding = 20
    
    blur {
        enabled = true 
        size = 40
        passes = 1
    }

    drop_shadow = yes
    shadow_range = 4
    shadow_render_power = 5
    col.shadow = rgba(1a1a1aee)
}

animations {
    enabled = yes

    # Some default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more

    bezier = myBezier, 0.47, 0.19, 0.41, 1

    animation = windows, 1, 7, myBezier
    animation = windowsOut, 1, 10, myBezier
    animation = windowsIn, 1, 10, myBezier
    animation = border, 1, 10, default
    animation = borderangle, 1, 10, myBezier 
    animation = fadeSwitch, 1, 10, myBezier 
    animation = workspaces, 1, 6, default
}

dwindle {
    # See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
    pseudotile = yes # master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = yes # you probably want this
}

master {
    # See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
    new_is_master = true
}

gestures {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more
    workspace_swipe = off
}

# Example per-device config
# See https://wiki.hyprland.org/Configuring/Keywords/#executing for more
device:epic-mouse-v1 {
    sensitivity = -0.5
}

# Example windowrule v1
# windowrule = float, ^(kitty)$
# Example windowrule v2
# windowrulev2 = float,class:^(kitty)$,title:^(kitty)$
# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more

# See https://wiki.hyprland.org/Configuring/Keywords/ for more
$mainMod = SUPER
$one = hyprctl hyprpaper wallpaper "DP-1, /home/admin/WallPapers/mandelbrot_green.png"

windowrule = opacity 1 override 0.6 override,^(kitty)$ 
windowrule = opacity 1 override 0.6 override,^(neovide)$ 

# windowrule = opacity 0.95 override 0.6 override,^(chrome)$
# windowrule = opacity 0.95 override 0.6 override,^(firefox)$
windowrule = noblur, ^(neovide)$
windowrule = opacity 0.95 override 0.6 override,^(Studio)$
windowrule = opacity 0.95 override 0.6 override,^(obsidian)$

bind = $mainMod SHIFT, S, exec, grim -g "$(slurp)" image.png | xclip -selection clipboard -t image/png -i image.png 

bind = $mainMod, U, exec, notify-send 'test'
bind = $mainMod, O, exec, kitty
bind = $mainMod, N, exec, neovide 
bind = $mainMod, Q, killactive, 
bind = $mainMod, E, exec, dolphin
bind = $mainMod, R, exec, wofi --show drun

bind = $mainMod SHIFT, h, movewindow, l
bind = $mainMod SHIFT, l, movewindow, r
bind = $mainMod SHIFT, k, movewindow, u
bind = $mainMod SHIFT, j, movewindow, d

bind = SUPERALT, H, resizeactive, l0 0
bind = SUPERALT, L, resizeactive, 10 0

# Move focus with mainMod 
bind = $mainMod, h, movefocus, l
bind = $mainMod, l, movefocus, r
bind = $mainMod, k, movefocus, u
bind = $mainMod, j, movefocus, d

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

env = LIBVA_DRIVER_NAME,nvidia
env = XDG_SESSION_TYPE,wayland
env = GBM_BACKEND,nvidia-drm
env = __GLX_VENDOR_LIBRARY_NAME,nvidia
env = WLR_NO_HARDWARE_CURSORS,1
